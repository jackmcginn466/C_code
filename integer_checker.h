#pragma once
#ifndef MAIN_HEADER  
#define MAIN_HEADER

//Function that keeps taking a user input until it is definetly an integer in the range provided, then returns this input
int integer_checker(int);

#endif
